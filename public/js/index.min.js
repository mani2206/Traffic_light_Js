const lights=document.querySelectorAll(".light");let currentIndex=0;function LightChange(){lights.forEach(light=>light.classList.remove("active")),lights[currentIndex].classList.add("active"),currentIndex=(currentIndex+1)%lights.length}lights[currentIndex].classList.add("active"),setInterval(LightChange,1e3);